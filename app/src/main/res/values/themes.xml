<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CW_HikerManagementApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CW_HikerManagementApp" parent="Base.Theme.CW_HikerManagementApp" />
    <style name="labelAdd" parent="Base.Theme.CW_HikerManagementApp">
        <item name="android:textSize">20dp</item>
        <item name="android:textColor">@color/black</item>
    </style>
    <style name="textBox" parent="Base.Theme.CW_HikerManagementApp">
        <item name="android:width">380dp</item>
        <item name="android:height">50dp</item>
        <item name="android:textSize">20dp</item>
    </style>
    <style name="textBoxMini" parent="Base.Theme.CW_HikerManagementApp">
        <item name="android:width">100dp</item>
        <item name="android:height">50dp</item>
        <item name="android:textSize">20dp</item>
    </style>
    <style name="CustomToolbarStyle" parent="Widget.AppCompat.Toolbar">
        <!-- Customize other attributes of the Toolbar if needed -->
        <item name="android:fontFamily">serif</item>
    </style>
</resources>